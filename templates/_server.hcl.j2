datacenter = "{{ consul_server.datacenter }}"

data_dir = "{{ consul_server.data_dir }}"

log_level = "{{ consul_server.log_level }}"

node_name = "{{ consul_server.node_name }}"

server = true

bootstrap_expect = 1

enable_script_checks = true
